<% if authenticated? %>
  <!-- Authenticated Dashboard View -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="text-center mb-12">
      <div class="mb-4">
        <% if Current.user.avatar_url.present? %>
          <img src="<%= Current.user.avatar_url %>" alt="<%= Current.user.name || Current.user.email_address %>"
               class="w-20 h-20 rounded-full mx-auto border-4 border-white shadow-lg"
               referrerpolicy="no-referrer"
               crossorigin="anonymous">
        <% else %>
          <div class="w-20 h-20 rounded-full mx-auto border-4 border-white shadow-lg bg-gray-300 flex items-center justify-center">
            <span class="text-lg font-medium text-gray-700">
              <%= (Current.user.name&.first || Current.user.email_address.first).upcase %>
            </span>
          </div>
        <% end %>
      </div>
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        Welcome back<%= Current.user.name.present? ? ", #{Current.user.name}!" : "!" %>
      </h1>
      <p class="text-xl text-gray-600">You're signed in as <%= Current.user.email_address %></p>
    </div>

    <!-- Dashboard Cards -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
            <%= icon 'chart-bar', class: 'w-6 h-6 text-blue-600' %>
          </div>
          <div class="ml-4">
            <h3 class="text-lg font-semibold text-gray-900">Analytics</h3>
            <p class="text-gray-600">View your metrics</p>
          </div>
        </div>
      </div>

      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
            <%= icon 'users', class: 'w-6 h-6 text-green-600' %>
          </div>
          <div class="ml-4">
            <h3 class="text-lg font-semibold text-gray-900">Team</h3>
            <p class="text-gray-600">Manage your team</p>
          </div>
        </div>
      </div>

      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center">
            <%= icon 'cog', class: 'w-6 h-6 text-purple-600' %>
          </div>
          <div class="ml-4">
            <h3 class="text-lg font-semibold text-gray-900">Settings</h3>
            <p class="text-gray-600">Configure your app</p>
          </div>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <!-- Landing Page for Unauthenticated Users -->
  <!-- Hero Section -->
  <div class="bg-gradient-to-r from-blue-600 to-purple-600">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24">
      <div class="text-center">
        <h1 class="text-4xl md:text-6xl font-bold text-white mb-6">
          Build Your SaaS<br>
          <span class="text-blue-200">Faster Than Ever</span>
        </h1>
        <p class="text-xl text-blue-100 mb-8 max-w-3xl mx-auto">
          Skip the boilerplate and focus on what matters. Our starter kit includes authentication, payments, and everything you need to launch your SaaS product.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <%= link_to "Get Started Free", signup_path, class: "bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-gray-50 transition-colors" %>
          <a href="#features" class="border border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-blue-600 transition-colors">
            Learn More
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Features Section -->
  <div id="features" class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Everything You Need</h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Launch faster with pre-built components, authentication, and integrations that would take weeks to implement from scratch.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="w-16 h-16 bg-blue-100 rounded-lg flex items-center justify-center mx-auto mb-4">
            <%= icon 'check-circle', class: 'w-8 h-8 text-blue-600' %>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">Authentication Ready</h3>
          <p class="text-gray-600">Complete auth system with OAuth, password reset, and session management built-in.</p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-4">
            <%= icon 'lightning-bolt', class: 'w-8 h-8 text-green-600' %>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">Lightning Fast</h3>
          <p class="text-gray-600">Built with Rails 8.1, Hotwire, and optimized for performance out of the box.</p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-purple-100 rounded-lg flex items-center justify-center mx-auto mb-4">
            <%= icon 'paint-brush', class: 'w-8 h-8 text-purple-600' %>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">Modern Design</h3>
          <p class="text-gray-600">Beautiful, responsive UI components built with Tailwind CSS and modern patterns.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- CTA Section -->
  <div class="bg-gray-50 py-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Ready to Build Something Amazing?</h2>
      <p class="text-xl text-gray-600 mb-8">Join thousands of developers who are shipping faster with our starter kit.</p>
      <%= link_to "Start Building Today", signup_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-lg font-semibold transition-colors" %>
    </div>
  </div>
<% end %>
