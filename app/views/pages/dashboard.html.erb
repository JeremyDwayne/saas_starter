<div class="bg-gray-50 min-h-screen py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-white shadow rounded-lg p-6">
      <h1 class="text-3xl font-bold text-gray-900 mb-6">Dashboard</h1>

      <div class="mb-8">
        <h2 class="text-xl font-semibold text-gray-800 mb-4">Welcome, <%= Current.user.name || Current.user.email_address %>!</h2>
        <p class="text-gray-600">Manage your account and subscription settings below.</p>
      </div>

      <% if Current.user.subscribed? %>
        <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
          <h3 class="text-lg font-medium text-green-800 mb-2">Active Subscription</h3>
          <p class="text-green-700">You have an active subscription. Thank you for being a customer!</p>

          <%= button_to "Cancel Subscription", cancel_subscription_subscriptions_path,
                method: :delete,
                class: "mt-4 bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium",
                confirm: "Are you sure you want to cancel your subscription?" %>
        </div>
      <% else %>
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
          <h3 class="text-lg font-medium text-blue-800 mb-2">No Active Subscription</h3>
          <p class="text-blue-700">Choose a plan to get started with premium features.</p>

          <%= link_to "View Plans", pricing_path,
                class: "mt-4 inline-block bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium" %>
        </div>
      <% end %>
    </div>
  </div>
</div>